# examples
plotBounds(boundsPredprob(
  looks = c(10, 20, 30, 40), p0 = 0.20, tT = 0.80,
  phiL = 0.10, phiU = 0.90,
), yt = "x")
plotBounds(boundsPredprob(
  looks = c(10, 20, 30, 40), p0 = 0.20, tT = 0.80,
  phiL = 0.10, phiU = 0.90,
), yt = "p")
